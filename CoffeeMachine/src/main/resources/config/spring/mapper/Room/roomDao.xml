<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="RoomVoMapper">
    
   <!--  <insert id="insertRoom" parameterType="com.ddstudio.coffeemachine.vo.RoomVo">
        INSERT INTO tb_room (
        
        )
        VALUES (
        
        );
    </insert>

    <select id="selectOne" parameterType="int" resultMap="com.ddstudio.coffeemachine.vo.RoomVo">
        SELECT * FROM tb_room WHERE roomId = #{roonId}
    </select> -->
     
</mapper>